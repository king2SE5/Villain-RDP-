name: RDP

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 350

    strategy:
      matrix:
        server: 
          - "China"
          - "Singapore"
          - "Russia"
          - "Japan"
          - "Spain"
          - "Canada"
          - "Norway"
          - "Italy"
          - "India"
          - "France"
          - "Germany"
          - "United Kingdom"
          - "United States"
          - "United Arab Emirates"

    steps:
      - name: Configure Core RDP Settings
        shell: powershell
        run: |
          # Similar RDP configuration code as before
          ...

      - name: Create RDP User
        shell: powershell
        run: |
          # Similar user creation code as before
          ...

      - name: Install VPN Client
        shell: powershell
        run: |
          # Download and install your chosen VPN client here
          # Example for OpenVPN or WireGuard
          ...

      - name: Connect to VPN
        shell: powershell
        run: |
          # Connect to the VPN using credentials stored in GitHub secrets
          # Example:
          # & "C:\Path\To\vpn\client.exe" -connect "your_vpn_config.ovpn"
          ...

      - name: Install Tailscale
        shell: powershell
        run: |
          # Tailscale installation as before
          ...

      - name: Connect Tailscale
        shell: powershell
        run: |
          # Tailscale connection as before
          ...

      - name: Verify RDP Port
        shell: powershell
        run: |
          # RDP port verification as before
          ...

      - name: Show Credentials & Keep Alive
        shell: powershell
        run: |
          # Show credentials as before
          ...
